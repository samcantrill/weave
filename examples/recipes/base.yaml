name: config-recipes-example
schema_version: 1
paths:
  work_dir: ./work
  output_dir: ${paths.work_dir}/outputs
settings:
  sample_count: 5
  retention_days: 7
  secret_token: local-token
pipeline:
  name: config-recipes-example
  retention:
    _recipe_: retention_policy
    days: ${settings.retention_days}
    tier: standard
  output_dir: ${paths.output_dir}

